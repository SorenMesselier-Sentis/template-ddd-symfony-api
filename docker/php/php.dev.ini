; Show raw PHP errors (syntax/fatal errors before Symfony's own handler is
; registered) instead of a blank page. opcache hot-reload is already the
; baseline in docker/php/opcache.ini, no dev-specific override needed there.
display_errors = On
