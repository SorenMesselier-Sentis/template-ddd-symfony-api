framework:
    messenger:
        default_bus: command.bus
        failure_transport: async.dead_letter
        buses:
            command.bus:
                middleware:
                    - App\Shared\Infrastructure\Monitoring\Metrics\BusMetricsMiddleware
                    - App\User\Infrastructure\Messaging\AuthorizeMessageMiddleware
                    - doctrine_transaction
            query.bus:
                middleware:
                    - App\Shared\Infrastructure\Monitoring\Metrics\BusMetricsMiddleware
                    - App\User\Infrastructure\Messaging\AuthorizeMessageMiddleware
            event.bus:
                default_middleware: allow_no_handlers
                middleware:
                    - App\Shared\Infrastructure\Monitoring\Metrics\BusMetricsMiddleware

        transports:
            sync: 'sync://'

            async:
                dsn: '%env(RABBITMQ_DSN)%'
                options:
                    exchange:
                        name: events
                        type: topic
                    queues:
                        events.user:
                            binding_keys: ['user.#']
                retry_strategy:
                    max_retries: 3
                    delay: 1000
                    multiplier: 2

            async.dead_letter:
                dsn: '%env(RABBITMQ_DSN)%'
                options:
                    exchange:
                        name: dead_letter
                        type: direct
                    queues:
                        dead_letter: ~
                retry_strategy:
                    max_retries: 0

        routing:
            'App\Shared\Domain\Bus\Command\Command': sync
            'App\Shared\Domain\Bus\Query\Query': sync
            'App\Shared\Domain\Bus\Event\DomainEvent': async
            'App\Shared\Infrastructure\Messaging\RabbitMQ\DeadLetterMessage': async.dead_letter
